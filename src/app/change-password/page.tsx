"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { KeyRound } from "lucide-react";
import { Button, FieldLabel, Panel, TextInput } from "@/components/ui";
export default function ChangePasswordPage() { const router=useRouter(); const [currentPassword,setCurrent]=useState(""); const [newPassword,setNew]=useState(""); const [confirm,setConfirm]=useState(""); const [error,setError]=useState(""); const [busy,setBusy]=useState(false); async function submit(e:React.FormEvent){e.preventDefault();if(newPassword!==confirm)return setError("The new passwords do not match.");setBusy(true);setError("");const r=await fetch("/api/auth/change-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({currentPassword,newPassword})});const d=await r.json();setBusy(false);if(!r.ok)return setError(d.error);router.replace("/");router.refresh();} return <div className="flex min-h-screen items-center justify-center px-4"><Panel className="w-full max-w-md p-7"><KeyRound className="mb-4 text-cyan-200"/><h1 className="text-2xl font-semibold text-white">Set a new password</h1><p className="mb-6 mt-2 text-sm text-slate-400">For security, change your temporary password before continuing.</p><form onSubmit={submit} className="grid gap-4">{error&&<div className="rounded-md bg-red-500/10 p-3 text-sm text-red-100">{error}</div>}<Pass label="Temporary password" value={currentPassword} set={setCurrent}/><Pass label="New password" value={newPassword} set={setNew}/><Pass label="Confirm new password" value={confirm} set={setConfirm}/><Button variant="primary" disabled={busy}>{busy?"Saving...":"Save password"}</Button></form></Panel></div> }
function Pass({label,value,set}:{label:string;value:string;set:(v:string)=>void}){return <label className="grid gap-2"><FieldLabel>{label}</FieldLabel><TextInput type="password" value={value} onChange={e=>set(e.target.value)} required/></label>}
